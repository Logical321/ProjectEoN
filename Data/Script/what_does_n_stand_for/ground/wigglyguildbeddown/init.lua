--[[
    init.lua
    Created: 06/15/2023 00:47:02
    Description: Autogenerated script file for the map wigglyguildbeddown.
]]--
-- Commonly included lua functions and data
require 'what_does_n_stand_for.common'

-- Package name
local wigglyguildbeddown = {}

-- Local, localized strings table
-- Use this to display the named strings you added in the strings files for the map!
-- Ex:
--      local localizedstring = STRINGS.MapStrings['SomeStringName']


-------------------------------
-- Map Callbacks
-------------------------------
---wigglyguildbeddown.Init(map)
--Engine callback function
function wigglyguildbeddown.Init(map)

  --This will fill the localized strings table automatically based on the locale the game is 
  -- currently in. You can use the MapStrings table after this line!
  

end

---wigglyguildbeddown.Enter(map)
--Engine callback function
function wigglyguildbeddown.Enter(map)

  GAME:FadeIn(20)

end

---wigglyguildbeddown.Exit(map)
--Engine callback function
function wigglyguildbeddown.Exit(map)


end

---wigglyguildbeddown.Update(map)
--Engine callback function
function wigglyguildbeddown.Update(map)


end

---wigglyguildbeddown.GameSave(map)
--Engine callback function
function wigglyguildbeddown.GameSave(map)


end

---wigglyguildbeddown.GameLoad(map)
--Engine callback function
function wigglyguildbeddown.GameLoad(map)

  GAME:FadeIn(20)

end

-------------------------------
-- Entities Callbacks
-------------------------------

function wigglyguildbeddown.InteractiveNearby_Touch(obj, activator)
	SOUND:PlayBattleSE("EVT_Emote_Confused")
    GROUND:CharSetEmote(activator, "question", 1)
end

function wigglyguildbeddown.Bucket_Action(obj, activator)
local player = CH('PLAYER')

UI:SetSpeaker(player)
UI:SetSpeakerEmotion("Inspired")
UI:WaitShowDialogue("(It's me!)")
UI:WaitShowDialogue("...")
UI:SetSpeakerEmotion("Normal")
UI:WaitShowDialogue("(Well, [pause=10]realistically, [pause=10]it's a reflection of me through water.)")

end

function wigglyguildbeddown.LeaveRoom_Touch(obj, activator)

GAME:FadeOut(false, 20)
GAME:EnterGroundMap("wigglyguildbedhalls", "EntranceDown")

end

return wigglyguildbeddown

